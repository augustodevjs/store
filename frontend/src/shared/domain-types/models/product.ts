export type productViewModel = {
  id: number;
  price: number;
  title: string;
  description: string;
}

export type FormProductInputModel = {
  price: number;
  title: string;
  description: string;
}

export type FormProductUpdateInputModel = {
  id: number;
  price: number;
  title: string;
  description: string;
}